import { AuthorSortPipe } from './author-sort.pipe';

describe('AuthorSortPipe', () => {
  it('create an instance', () => {
    const pipe = new AuthorSortPipe();
    expect(pipe).toBeTruthy();
  });
});
